diff -ruN '--exclude=.git' a/backtrader/brokers/bbroker.py b/backtrader/brokers/bbroker.py
--- a/backtrader/brokers/bbroker.py	2026-09-03 11:13:03.034468500 -0600
+++ b/backtrader/brokers/bbroker.py	2026-09-03 11:31:05.374600600 -0600
@@ -429,9 +429,17 @@
             self._fundshares += c / self._fundval
             self.cash += c
 
+        # PERF: when valuing the whole book, skip flat positions. Every
+        # comminfo term below is proportional to size, so a size-0 position
+        # contributes exactly 0.0 to each accumulator (adding 0.0 to a float
+        # is an identity), and the per-data early return only applies when
+        # datas was given.
+        skipflat = not datas
         for data in datas or self.positions:
-            comminfo = self.getcommissioninfo(data)
             position = self.positions[data]
+            if skipflat and not position.size:
+                continue
+            comminfo = self.getcommissioninfo(data)
             # use valuesize:  returns raw value, rather than negative adj val
             if not self.p.shortcash:
                 dvalue = comminfo.getvalue(position, data.close[0])
@@ -1183,7 +1191,7 @@
         # Discount any cash for positions hold
         credit = 0.0
         for data, pos in self.positions.items():
-            if pos:
+            if pos.size:
                 comminfo = self.getcommissioninfo(data)
                 dt0 = data.datetime.datetime()
                 dcredit = comminfo.get_credit_interest(data, pos, dt0)
@@ -1222,7 +1230,7 @@
         # Operations have been executed ... adjust cash end of bar
         for data, pos in self.positions.items():
             # futures change cash every bar
-            if pos:
+            if pos.size:
                 comminfo = self.getcommissioninfo(data)
                 self.cash += comminfo.cashadjust(pos.size,
                                                  pos.adjbase,
diff -ruN '--exclude=.git' a/backtrader/cerebro.py b/backtrader/cerebro.py
--- a/backtrader/cerebro.py	2026-09-03 11:13:03.300369900 -0600
+++ b/backtrader/cerebro.py	2026-09-03 11:31:05.417125100 -0600
@@ -330,7 +330,7 @@
         for elem in iterable:
             if isinstance(elem, string_types):
                 elem = (elem,)
-            elif not isinstance(elem, collections.Iterable):
+            elif not isinstance(elem, collections.abc.Iterable):
                 elem = (elem,)
 
             niterable.append(elem)
diff -ruN '--exclude=.git' a/backtrader/feed.py b/backtrader/feed.py
--- a/backtrader/feed.py	2026-09-03 11:13:02.957124300 -0600
+++ b/backtrader/feed.py	2026-09-03 11:31:05.364162900 -0600
@@ -106,6 +106,14 @@
         _obj._barstack = collections.deque()  # for filter operations
         _obj._barstash = collections.deque()  # for filter operations
 
+        # PERF: the tick_xxx attribute names and their line indices, computed
+        # once instead of rebuilt through getlinealiases()/getattr on every
+        # bar of every data in _tick_nullify/_tick_fill
+        _aliases = _obj.getlinealiases()
+        _obj._tickattrs = tuple(('tick_' + a, i) for i, a in enumerate(_aliases)
+                                if a != 'datetime')
+        _obj._tickattr0 = 'tick_' + _obj._getlinealias(0)
+
         _obj._filters = list()
         _obj._ffilters = list()
         for fp in _obj.p.filters:
@@ -349,30 +357,44 @@
         # and the length doesn't change like if a replay is happening or
         # a real-time data feed is in use and 1 minutes bars are being
         # constructed with 5 seconds updates
-        for lalias in self.getlinealiases():
-            if lalias != 'datetime':
-                setattr(self, 'tick_' + lalias, None)
+        for tname, _ in self._tickattrs:
+            setattr(self, tname, None)
 
         self.tick_last = None
 
     def _tick_fill(self, force=False):
         # If nothing filled the tick_xxx attributes, the bar is the tick
-        alias0 = self._getlinealias(0)
-        if force or getattr(self, 'tick_' + alias0, None) is None:
-            for lalias in self.getlinealiases():
-                if lalias != 'datetime':
-                    setattr(self, 'tick_' + lalias,
-                            getattr(self.lines, lalias)[0])
+        if force or getattr(self, self._tickattr0, None) is None:
+            lines = self.lines.lines
+            for tname, i in self._tickattrs:
+                line = lines[i]
+                setattr(self, tname, line.array[line._idx])
 
-            self.tick_last = getattr(self.lines, alias0)[0]
+            line = lines[0]
+            self.tick_last = line.array[line._idx]
 
     def advance_peek(self):
-        if len(self) < self.buflen():
-            return self.lines.datetime[1]  # return the future
+        line0 = self.lines.lines[0]
+        if line0.lencount < len(line0.array) - line0.extension:
+            dtline = self.lines.datetime
+            return dtline.array[dtline._idx + 1]  # return the future
 
         return float('inf')  # max date else
 
     def advance(self, size=1, datamaster=None, ticks=True):
+        if datamaster is None:
+            # PERF: the common preloaded/runonce path. Nullify-then-fill
+            # leaves the same state as a forced fill, and nothing reads the
+            # tick_xxx attributes in between, so only the branch that would
+            # have ended without a fill (past the last bar) still nullifies.
+            self.lines.advance(size)
+            if ticks:
+                if len(self) < self.buflen():
+                    self._tick_fill(force=True)
+                else:
+                    self._tick_nullify()
+            return
+
         if ticks:
             self._tick_nullify()
 
diff -ruN '--exclude=.git' a/backtrader/feeds/pandafeed.py b/backtrader/feeds/pandafeed.py
--- a/backtrader/feeds/pandafeed.py	2026-09-03 11:13:02.159742100 -0600
+++ b/backtrader/feeds/pandafeed.py	2026-09-03 11:31:05.256316300 -0600
@@ -231,41 +231,47 @@
 
             self._colmapping[k] = v
 
-    def _load(self):
-        self._idx += 1
-
-        if self._idx >= len(self.p.dataname):
-            # exhausted all rows
-            return False
-
-        # Set the standard datafields
-        for datafield in self.getlinealiases():
+        # PERF: pull each mapped column out of the frame once. The original
+        # _load did DataFrame.iloc[row, col] per cell per bar, which is ~25
+        # microseconds of pandas indexing machinery for a single float and
+        # dominated the whole preload. Values are identical: iloc[:, col]
+        # keeps the column dtype, so element i is the same scalar
+        # iloc[i, col] would have produced.
+        df = self.p.dataname
+        self._nrows = len(df)
+        self._colarrays = []
+        for lidx, datafield in enumerate(self.getlinealiases()):
             if datafield == 'datetime':
                 continue
-
             colindex = self._colmapping[datafield]
             if colindex is None:
-                # datafield signaled as missing in the stream: skip it
-                continue
-
-            # get the line to be set
-            line = getattr(self.lines, datafield)
+                continue  # datafield signaled as missing in the stream
+            self._colarrays.append((lidx, df.iloc[:, colindex].to_numpy()))
 
-            # indexing for pandas: 1st is colum, then row
-            line[0] = self.p.dataname.iloc[self._idx, colindex]
-
-        # datetime conversion
         coldtime = self._colmapping['datetime']
-
         if coldtime is None:
-            # standard index in the datetime
-            tstamp = self.p.dataname.index[self._idx]
+            dtvalues = df.index
         else:
-            # it's in a different column ... use standard column index
-            tstamp = self.p.dataname.iloc[self._idx, coldtime]
+            dtvalues = df.iloc[:, coldtime]
+        # positional scalar access, same objects as index[i] /
+        # iloc[i, coldtime] (pandas Timestamps with to_pydatetime)
+        self._dtvalues = list(dtvalues)
+
+    def _load(self):
+        self._idx += 1
+        idx = self._idx
+
+        if idx >= self._nrows:
+            # exhausted all rows
+            return False
+
+        # Set the standard datafields
+        lines = self.lines.lines
+        for lidx, colarray in self._colarrays:
+            lines[lidx][0] = colarray[idx]
 
-        # convert to float via datetime and store it
-        dt = tstamp.to_pydatetime()
+        # datetime conversion: convert to float via datetime and store it
+        dt = self._dtvalues[idx].to_pydatetime()
         dtnum = date2num(dt)
         self.lines.datetime[0] = dtnum
 
diff -ruN '--exclude=.git' a/backtrader/linebuffer.py b/backtrader/linebuffer.py
--- a/backtrader/linebuffer.py	2026-09-03 11:13:02.960180500 -0600
+++ b/backtrader/linebuffer.py	2026-09-03 11:31:05.365866200 -0600
@@ -34,7 +34,7 @@
 import array
 import collections
 import datetime
-from itertools import islice
+from itertools import islice, repeat
 import math
 
 from .utils.py3 import range, with_metaclass, string_types
@@ -160,7 +160,7 @@
         return len(self.array) - self.extension
 
     def __getitem__(self, ago):
-        return self.array[self.idx + ago]
+        return self.array[self._idx + ago]
 
     def get(self, ago=0, size=1):
         ''' Returns a slice of the array relative to *ago*
@@ -176,12 +176,13 @@
         Returns:
             A slice of the underlying buffer
         '''
+        idx = self._idx
         if self.useislice:
-            start = self.idx + ago - size + 1
-            end = self.idx + ago + 1
+            start = idx + ago - size + 1
+            end = idx + ago + 1
             return list(islice(self.array, start, end))
 
-        return self.array[self.idx + ago - size + 1:self.idx + ago + 1]
+        return self.array[idx + ago - size + 1:idx + ago + 1]
 
     def getzeroval(self, idx=0):
         ''' Returns a single value of the array relative to the real zero
@@ -219,7 +220,7 @@
             the slice
             value (variable): value to be set
         '''
-        self.array[self.idx + ago] = value
+        self.array[self._idx + ago] = value
         for binding in self.bindings:
             binding[ago] = value
 
@@ -231,7 +232,7 @@
             ago (int): Point of the array to which size will be added to return
             the slice
         '''
-        self.array[self.idx + ago] = value
+        self.array[self._idx + ago] = value
         for binding in self.bindings:
             binding[ago] = value
 
@@ -251,11 +252,16 @@
             value (variable): value to be set in new positins
             size (int): How many extra positions to enlarge the buffer
         '''
-        self.idx += size
+        if self.mode == self.UnBounded:
+            self._idx += size
+        else:
+            self.set_idx(self._idx + size)
         self.lencount += size
 
-        for i in range(size):
+        if size == 1:
             self.array.append(value)
+        else:
+            self.array.extend(repeat(value, size))
 
     def backwards(self, size=1, force=False):
         ''' Moves the logical index backwards and reduces the buffer as much as needed
@@ -271,7 +277,10 @@
             self.array.pop()
 
     def rewind(self, size=1):
-        self.idx -= size
+        if self.mode == self.UnBounded:
+            self._idx -= size
+        else:
+            self.set_idx(self._idx - size)
         self.lencount -= size
 
     def advance(self, size=1):
@@ -280,7 +289,10 @@
         Keyword Args:
             size (int): How many extra positions to move forward
         '''
-        self.idx += size
+        if self.mode == self.UnBounded:
+            self._idx += size
+        else:
+            self.set_idx(self._idx + size)
         self.lencount += size
 
     def extend(self, value=NAN, size=0):
@@ -294,8 +306,10 @@
         set values in the buffer "future"
         '''
         self.extension += size
-        for i in range(size):
+        if size == 1:
             self.array.append(value)
+        elif size > 1:
+            self.array.extend(repeat(value, size))
 
     def addbinding(self, binding):
         ''' Adds another line binding
@@ -384,22 +398,22 @@
         self._tz = tz
 
     def datetime(self, ago=0, tz=None, naive=True):
-        return num2date(self.array[self.idx + ago],
+        return num2date(self.array[self._idx + ago],
                         tz=tz or self._tz, naive=naive)
 
     def date(self, ago=0, tz=None, naive=True):
-        return num2date(self.array[self.idx + ago],
+        return num2date(self.array[self._idx + ago],
                         tz=tz or self._tz, naive=naive).date()
 
     def time(self, ago=0, tz=None, naive=True):
-        return num2date(self.array[self.idx + ago],
+        return num2date(self.array[self._idx + ago],
                         tz=tz or self._tz, naive=naive).time()
 
     def dt(self, ago=0):
         '''
         return numeric date part of datetimefloat
         '''
-        return math.trunc(self.array[self.idx + ago])
+        return math.trunc(self.array[self._idx + ago])
 
     def tm_raw(self, ago=0):
         '''
@@ -408,7 +422,7 @@
         # This function is named raw because it retrieves the fractional part
         # without transforming it to time to avoid the influence of the day
         # count (integer part of coding)
-        return math.modf(self.array[self.idx + ago])[0]
+        return math.modf(self.array[self._idx + ago])[0]
 
     def tm(self, ago=0):
         '''
@@ -417,7 +431,7 @@
         # To avoid precision errors, this returns the fractional part after
         # having converted it to a datetime.time object to avoid precision
         # errors in comparisons
-        return time2num(num2date(self.array[self.idx + ago]).time())
+        return time2num(num2date(self.array[self._idx + ago]).time())
 
     def tm_lt(self, other, ago=0):
         '''
@@ -426,7 +440,7 @@
         # To compare a raw "tm" part (fractional part of coded datetime)
         # with the tm of the current datetime, the raw "tm" has to be
         # brought in sync with the current "day" count (integer part) to avoid
-        dtime = self.array[self.idx + ago]
+        dtime = self.array[self._idx + ago]
         tm, dt = math.modf(dtime)
 
         return dtime < (dt + other)
@@ -438,7 +452,7 @@
         # To compare a raw "tm" part (fractional part of coded datetime)
         # with the tm of the current datetime, the raw "tm" has to be
         # brought in sync with the current "day" count (integer part) to avoid
-        dtime = self.array[self.idx + ago]
+        dtime = self.array[self._idx + ago]
         tm, dt = math.modf(dtime)
 
         return dtime <= (dt + other)
@@ -450,7 +464,7 @@
         # To compare a raw "tm" part (fractional part of coded datetime)
         # with the tm of the current datetime, the raw "tm" has to be
         # brought in sync with the current "day" count (integer part) to avoid
-        dtime = self.array[self.idx + ago]
+        dtime = self.array[self._idx + ago]
         tm, dt = math.modf(dtime)
 
         return dtime == (dt + other)
@@ -462,7 +476,7 @@
         # To compare a raw "tm" part (fractional part of coded datetime)
         # with the tm of the current datetime, the raw "tm" has to be
         # brought in sync with the current "day" count (integer part) to avoid
-        dtime = self.array[self.idx + ago]
+        dtime = self.array[self._idx + ago]
         tm, dt = math.modf(dtime)
 
         return dtime > (dt + other)
@@ -474,7 +488,7 @@
         # To compare a raw "tm" part (fractional part of coded datetime)
         # with the tm of the current datetime, the raw "tm" has to be
         # brought in sync with the current "day" count (integer part) to avoid
-        dtime = self.array[self.idx + ago]
+        dtime = self.array[self._idx + ago]
         tm, dt = math.modf(dtime)
 
         return dtime >= (dt + other)
@@ -485,7 +499,7 @@
 
         Useful for external comparisons to avoid precision errors
         '''
-        return int(self.array[self.idx + ago]) + tm
+        return int(self.array[self._idx + ago]) + tm
 
     def tm2datetime(self, tm, ago=0):
         '''
@@ -493,7 +507,7 @@
 
         Useful for external comparisons to avoid precision errors
         '''
-        return num2date(int(self.array[self.idx + ago]) + tm)
+        return num2date(int(self.array[self._idx + ago]) + tm)
 
 
 class MetaLineActions(LineBuffer.__class__):
diff -ruN '--exclude=.git' a/backtrader/lineiterator.py b/backtrader/lineiterator.py
--- a/backtrader/lineiterator.py	2026-09-03 11:13:02.961751100 -0600
+++ b/backtrader/lineiterator.py	2026-09-03 11:31:05.365866200 -0600
@@ -226,7 +226,7 @@
 
         if isinstance(owner, string_types):
             owner = [owner]
-        elif not isinstance(owner, collections.Iterable):
+        elif not isinstance(owner, collections.abc.Iterable):
             owner = [owner]
 
         if not own:
@@ -234,7 +234,7 @@
 
         if isinstance(own, string_types):
             own = [own]
-        elif not isinstance(own, collections.Iterable):
+        elif not isinstance(own, collections.abc.Iterable):
             own = [own]
 
         for lineowner, lineown in zip(owner, own):
diff -ruN '--exclude=.git' a/backtrader/lineseries.py b/backtrader/lineseries.py
--- a/backtrader/lineseries.py	2026-09-03 11:13:02.963267600 -0600
+++ b/backtrader/lineseries.py	2026-09-03 11:31:05.367385500 -0600
@@ -217,7 +217,7 @@
         '''
         Proxy line operation
         '''
-        return len(self.lines[0])
+        return self.lines[0].lencount
 
     def size(self):
         return len(self.lines) - self._getlinesextra()
@@ -292,8 +292,15 @@
         '''
         Proxy line operation
         '''
+        # PERF: inline LineBuffer.advance for unbounded buffers (a method
+        # call per line per bar otherwise); QBuffer lines keep the call so
+        # set_idx's clamp semantics are untouched
         for line in self.lines:
-            line.advance(size)
+            if line.mode == 0:  # LineBuffer.UnBounded
+                line._idx += size
+                line.lencount += size
+            else:
+                line.advance(size)
 
     def buflen(self, line=0):
         '''
@@ -437,6 +444,18 @@
             setattr(_obj, 'line_%d' % l, line)
             setattr(_obj, 'line%d' % l, line)
 
+        # PERF: direct references to the named lines. Reading data.close
+        # used to go LineSeries.__getattr__ -> getattr(lines, name) ->
+        # LineAlias.__get__ on every access; an instance attribute is one
+        # dictionary hit. Only aliases the class does not already define are
+        # published, so attribute resolution order is unchanged for the rest.
+        # LineSeriesStub replaces self.lines in __init__ but declares no
+        # named lines, so nothing stale is published for it.
+        lines_obj = _obj.lines
+        for l, linealias in enumerate(lines_obj._getlines()):
+            if linealias and not hasattr(cls, linealias):
+                setattr(_obj, linealias, lines_obj.lines[l])
+
         # Parameter values have now been set before __init__
         return _obj, args, kwargs
 
@@ -461,7 +480,7 @@
         return getattr(self.lines, name)
 
     def __len__(self):
-        return len(self.lines)
+        return self.lines.lines[0].lencount
 
     def __getitem__(self, key):
         return self.lines[0][key]
diff -ruN '--exclude=.git' a/backtrader/writer.py b/backtrader/writer.py
--- a/backtrader/writer.py	2026-09-03 11:13:03.271057300 -0600
+++ b/backtrader/writer.py	2026-09-03 11:31:05.408834800 -0600
@@ -205,7 +205,7 @@
                     self.writelineseparator(level=level)
                 self.writeline(kline)
                 self.writedict(val, level=level + 1, recurse=True)
-            elif isinstance(val, (list, tuple, collections.Iterable)):
+            elif isinstance(val, (list, tuple, collections.abc.Iterable)):
                 line = ', '.join(map(str, val))
                 self.writeline(kline + ' ' + line)
             else:
